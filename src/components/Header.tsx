import { Typography } from "@mui/material";

export default function Header() {
  return (
    <header>
      <Typography variant="h1" component="h1">
        Text to Brain
      </Typography>
    </header>
  );
}
